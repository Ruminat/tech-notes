### Bash

**My aliases**:

```bash
alias lnt="npm run lint"
alias e2e="npm run e2e"
alias tst="npm run test"
alias tch="npm run typecheck"
alias cdch="npm run codecheck"
alias dev="npm run dev"
alias bld="npm run build"
alias idev="npm i && npm run build"
alias cidev="npm ci && npm run build"
alias pidev="arc pull && npm i && npm run build"
alias pcidev="arc pull && npm ci && npm run build"
alias trunk="arc co trunk && arc pull"
```
